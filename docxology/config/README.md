# docxology/config

Optional YAML defaults for docxology runners.

| File | Consumer |
|------|----------|
| [orchestration.yaml](orchestration.yaml) | [scripts/run_docxology_orchestrations.py](../scripts/run_docxology_orchestrations.py) — `seed`, `fast`, `skip_heavy`, `verbose` |

Requires `pyyaml` (docxology `test` dependency group). See [AGENTS.md](AGENTS.md).
