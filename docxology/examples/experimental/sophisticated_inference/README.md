# examples/experimental/sophisticated_inference

Validated, verified real `pymdp` configurable functional validation layer for `experimental/sophisticated_inference`.

Outputs are signposted to `docxology/output/experimental/sophisticated_inference/` and tracked by the `run_all.py` pipeline.

## Parent

[../README.md](../README.md)
